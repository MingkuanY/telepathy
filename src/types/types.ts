export type WeatherType = {
  long_text: string; // The full weather report as a string
  summary: string; // A summarized version of the weather report
};

export type JokeType = {
  joke: string;
};
